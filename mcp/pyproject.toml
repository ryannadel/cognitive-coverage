[project]
name = "cognitive-coverage-mcp"
version = "0.1.0"
description = "MCP stdio server for Cognitive Coverage manifests"
requires-python = ">=3.10"
dependencies = [
  "mcp>=1.2.0,<2"
]

[dependency-groups]
dev = [
  "pytest>=8.0"
]
